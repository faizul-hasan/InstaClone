import { StyleSheet } from 'react-native';
import { fontSizes, fonts } from '@utilities/constants/common';

export const TabBarStyle = StyleSheet.create({
  tabBarlabel: {
    fontSize: fontSizes.small,
    fontFamily: fonts.PrimaryMedium,
  },
});
