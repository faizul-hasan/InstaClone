import { View, Text } from 'react-native';
import React from 'react';
import { ReelsStyles } from './styles';

const ReelsScreen = () => {
  return (
    <View style={ReelsStyles.container}>
      <Text>ReelsScreen</Text>
    </View>
  );
};

export default ReelsScreen;
